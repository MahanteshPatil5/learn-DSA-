#include<stdio.h>
#define max 10
int top,stac[max];
int main(){
    int n;
    printf("Enter the number of elemsts of stack : ");
    scanf("%d",&n);
    printf("Enter the elements of stack max 10: ");
    for(int i=0;i<n;i++){
        scanf("%d",&stac[i]);
    }
    printf("");

    
}